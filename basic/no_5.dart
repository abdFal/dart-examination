/** Booleans
 * Create a program that compares two numbers and
 * prints whether the first number is greater than the second.
 * Use boolean variables to store and print the result.
 * 
 */
void main() {
  var firstNumber = 1;
  var secNumber = 5;
  print(firstNumber > secNumber);
}
