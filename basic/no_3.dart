/** Number
 * Write a program that calculates the area of a rectangle.
 * Take the length and width as input (assume they are integers)
 * and output the calculated area.
 * 
 */
void main() {
  var length = 100;
  var width = 50;
  var result = length * width;
  print(result);
}
